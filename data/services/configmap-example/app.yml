---
$schema: /app-sre/app-1.yml

labels:
  service: example

name: configmap-example

description: |
  An example app file deploying a configmap

escalationPolicy:
  $ref: /teams/placeholder-escalation.yml
onboardingStatus: OnBoarded
serviceOwners:
- name: Example team
  email: owners@example.com
grafanaUrls:
  - title: 'Title'
    url: 'http://localhost:8080'
sopsUrl: 'https://example.com/sops'
architectureDocument: ''
